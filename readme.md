# Just quick node server hosting and accessing the database of movie torrent( which was recently closed). Data is 800MB+ and is not pushed to repo

## Run 
>npm install
>npm start

### add the database in db folder with name rarbg_db.sqlite